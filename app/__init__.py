"""Сервер ПОСТЕР.

Слои: core (база, права, логи) → models → schemas → services (правила без
HTTP) → api/v1 (ручки). Импорт ядра здесь — чтобы .env был прочитан раньше,
чем любой модуль посмотрит в переменные окружения, с какой бы стороны его
ни импортировали: сервер, скрипт или тест.
"""

from app.core import paths as _paths  # noqa: F401

#!/usr/bin/env bash
# Скрипт переехал в deploy/crm/poster.sh (в корне репозитория): развёртывание
# системы и сайта теперь в одной папке deploy/. Этот файл оставлен, чтобы уже
# установленные служба, сторож в cron и привычные команды продолжили работать
# после обновления. Новые установки его не используют.
exec bash "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/../../deploy/crm/poster.sh" "$@"

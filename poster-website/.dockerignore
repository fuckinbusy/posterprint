# В образ сборки — только исходники: зависимости ставятся внутри (npm ci),
# а node_modules с компьютера разработчика (особенно с Windows) там не
# заработают — бинарники сборщика у каждой системы свои.
node_modules/
dist/
*.tsbuildinfo
.vscode/
.idea/

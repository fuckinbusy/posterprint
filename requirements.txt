# Версии зафиксированы верхней границей: мажорный релиз FastAPI или Pydantic
# не должен приезжать сам по себе при установке на новой машине.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
pydantic>=2.7,<3.0
python-dotenv>=1.0,<2.0
# нужен FastAPI для приёма файлов (загрузка макетов, app/routers/designs.py):
# без него загрузка падает с невнятной ошибкой про форму
python-multipart>=0.0.9,<1.0

# для перехода на Postgres на сервере:
# psycopg[binary]>=3.1
